/**
 * Implementações auxiliares para testes e bootstrap local.
 * Não importar de `@repo/tenancy` na raiz — usar `@repo/tenancy/testing`.
 */
export { InMemoryTenantRepository } from './infrastructure/repositories/in-memory-tenant.repository.js'
